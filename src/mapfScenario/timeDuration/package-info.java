/**
 *
 * Time duration or rather Action is used to serve Solution tab Actions, it allows to rewrite agent action times,
 * so they are not as them picat defined.
 * ActionDurations are splited into categories, and user can choose category which will be used to rewrite durations.
 * On save durations are saved to configuration file.
 * To apply that choice it uses checkbock "use theese".
 *
 * *
 * */


package mapfScenario.timeDuration;